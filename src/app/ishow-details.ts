export interface IShowDetails { //This is the data we want to show.
    showName: string //changed from name to showName
    yearStart: number, //changed to number to only collect year
    yearEnd: number, 
    description: string
    image: string 
}
